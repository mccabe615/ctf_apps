<h1> Admin Portal </h1>
<img src="/assets/bamboo.jpg" alt="background image" id="bg" />
<div id="user_nav" style="position: absolute; top: 0; right: 0; width: auto; text-align:right;">
  <% if current_user %>
    <!--
    #This is my XSS-able version
    Logged in as <%= raw(current_user.email) %>.
    -->
    Logged in as <%= current_user.email %>.
    <%= link_to "Log out", log_out_path, :class => "a" %>
  <% end %>
</div>   
 

 <%= button_to "DB Maint.", {:controller => "admin", :action => "maint" }, {:class => "buttons" } %>
 <%= button_to "User Mgmt.", {:controller => "admin", :action => "mgmt" }, {:class => "buttons" } %>


<style type="text/css" media="screen">

body
{
background-color:#E8E8E8;
}
h1
{
color:#FFFFFF;
text-align:center;
font-family:"Times New Roman";
font-size:100px;
position:relative;
z-index:1;
}
#user_nav
{
color:#FFFFFF ;
position:relative;
z-index:1;
}
img#bg
{
position:fixed;
top:0;
left:0;
width:100%;
height:100%;
}
.buttons
{
color:#336666;
width: 300px;
font-size:25px;
position:relative;
z-index:1;
}
a {
  color: #FFFFFF;
  text-decoration: underline;
}

a:hover {
  color: #FFFFFF;
  text-decoration: underline;
}

a:visited {
  color: #FFFFFF;
