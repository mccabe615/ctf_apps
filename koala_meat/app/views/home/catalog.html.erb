<h1> KoalaMeat!</h1>


  <td>  <%= button_to "Home", {:action => "index"}, {:method => "get"}%></td>
   <td> <%= button_to "Catalog", {:action => "catalog"}, {:method => "get"}%></td>
   <td> <form method="post" action="mailto:help@koala_meat">
        <%= button_to "Contact Us", nil, {:method => "get"}%>
    </form>
   </td>
    <td><%= button_to "About Us", {:action => "about_us"}, {:method => "get"}%></td>


<table id="table" align="center" border="0" cellpadding="5" cellspacing="5" width="50%">
  <tr>
    <% @photos[0..2].each_with_index do |photo, idx| %>
     <%idx = idx + 1 %>
    <td>
      <%= image_tag(photo) %> 
      <p><%= link_to "KM-Item \##{idx}", show_catalog_item_path(:cat_id => "#{idx}") %><p>
    </td>
    <%end%>
  </tr>
  <tr>
    <% @photos[3..5].each_with_index do |photo, idx| %>
     <%idx = idx + 4 %>
    <td>
      <%= image_tag(photo) %> 
      <p><%= link_to "KM-Item \##{idx}", show_catalog_item_path(:cat_id => "#{idx}") %><p>
    </td>
    <%end%>
  </tr>
  <tr>
    <% @photos[6..8].each_with_index do |photo, idx| %>
     <%idx = idx + 7 %>
    <td>
      <%= image_tag(photo) %> 
      <p><%= link_to "KM-Item \##{idx}", show_catalog_item_path(:cat_id => "#{idx}") %><p>
    </td>
    <%end%>
  </tr>  
</table>