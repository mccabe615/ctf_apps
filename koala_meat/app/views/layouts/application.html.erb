<!DOCTYPE html>
<html>
<head>
  <title>Koala Meat</title>
  <!--[if lt IE 9]>
    
  <![endif]-->

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= stylesheet_link_tag("bootstrap_and_overrides")%>
  <%= csrf_meta_tags %>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">


 	<div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
		<div class="container-fluid">
			<div class="navbar-search pull-right">
				<ul class="nav">
					<% if not current_user %>
						<li><%= link_to "Login", log_in_path, {:style => "color: #FFFFFF"} %></li>
						<li><%= link_to "Signup", sign_up_path, {:style => "color: #FFFFFF"} %></li>
					<% else %>
						 <!-- begin admin code -->
						   <% if is_admin? %>
						   <li><%= link_to "Administration", admin_path, {:style => "color: #FFFFFF"}   %></li>
						   <% end %>
						 <!-- end -->
						<li><%= link_to "Logout", log_out_path, {:style => "color: #FFFFFF"}   %></li>
					<% end %>
				</ul>
			</div>
			<ul class="nav nav-pills">
		 		<a class="brand" style="color: #FFFFFF">Koala Meat</a>
					<li><%= link_to "Home", root_path, {:class => "active", :style => "color: #FFFFFF"}%></li>
			</ul>
		</div>
      </div>
    </div>
</head>
<body>
  <div class="container-fluid">
  		<%= yield %>
	</div>
</body>

</html>